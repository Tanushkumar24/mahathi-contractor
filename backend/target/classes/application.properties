# ===========================================
# BASE Configuration
# ===========================================

server.port=${PORT:8080}
spring.application.name=mahathi-contractor-api

# Admin Email
mahathi.admin.email=mahathicontractor@gmail.com

# Email DISABLED by default
mahathi.email.enabled=false

# ===========================================
# LOCAL DATABASE (for development)
# ===========================================
spring.datasource.url=jdbc:mysql://localhost:3306/mahathi_contractor?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Tanush098.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mahathicontractor@gmail.com
spring.mail.password=jqomckwzgcsnmbrm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# CORS
spring.mvc.cors.allowed-origins=*

# Logging
logging.level.com.mahathi.contractor=INFO
